# Theory

Run from the repository root:

```sh
python theory/make_figure.py
python theory/make_social_circuit.py
python theory/validate.py
```

Outputs: `paper/figures/final/fig8.*` and `fig6.*`.

Recompute the model probability grid:

```sh
python theory/model.py --recompute
```
