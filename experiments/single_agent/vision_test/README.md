# Vision test

Run from the repository root:

```sh
flag-game probe vision --backend scripted --model gpt-4o --truth-country Peru --limit-pairs 2 --render-scale 1 --out runs/vision_smoke
flag-game probe vision --help
```
