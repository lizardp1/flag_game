# Install after the base RunPod bootstrap:
#   source .venv/bin/activate
#   python -m pip install -r requirements-open-models.txt
#
# Use a RunPod PyTorch/CUDA template so torch is already matched to the image's
# CUDA stack. This file intentionally avoids reinstalling torch/torchvision.
-r requirements-runpod.txt

accelerate>=1.1.0
blobfile>=3.0.0
qwen-vl-utils>=0.0.10
safetensors>=0.4.5
tiktoken>=0.7.0
transformers>=4.51.3
torchvision>=0.20.0

# Optional for Qwen3-VL experiments if your image does not already have a new
# enough Transformers build:
#   python -m pip install -U git+https://github.com/huggingface/transformers accelerate
