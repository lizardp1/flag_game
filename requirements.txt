# Core data, config, and plotting dependencies for the flag-game and number-game code.
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
pydantic>=2.0
PyYAML>=6.0
Pillow>=10.0

# Original flag-game API backends.
openai>=1.0
anthropic>=0.34

# Local open-weight model execution for Qwen/Kimi-style Hugging Face runs.
# Keep torch and transformers in sync; older torch builds can fail with newer
# transformers imports such as torch.distributed.tensor.DTensor.
torch>=2.6
transformers>=4.53
accelerate>=1.0
safetensors>=0.4.5
huggingface-hub>=0.25
sentencepiece>=0.2
protobuf>=4.25
einops>=0.8
